---
phase: 10-pattern-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cadenza/patterns/__init__.py
  - src/cadenza/patterns/rhythm.py
  - src/cadenza/patterns/melodic.py
  - tests/patterns/__init__.py
  - tests/patterns/conftest.py
  - tests/patterns/test_rhythm.py
  - tests/patterns/test_melodic.py
autonomous: true
requirements: [RHYT-06, PATT-01, PATT-02, PATT-03, PATT-04, PATT-07]

must_haves:
  truths:
    - "euclidean_rhythm(3, 8) returns the tresillo pattern with exactly 3 True values in 8 slots"
    - "binary_rhythm(0b10110) returns (True, False, True, True, False)"
    - "apply_rhythm cycles pitches when fewer pitches than True slots in bitmap"
    - "isorhythm independently cycles talea and color until n LCM cycles complete"
    - "ostinato repeats a phrase n times, applying variation callback per repetition"
    - "accent_pattern marks every Nth Note with 'accent' articulation, skipping Rests"
  artifacts:
    - path: "src/cadenza/patterns/__init__.py"
      provides: "Package init with re-exports"
      contains: "euclidean_rhythm"
    - path: "src/cadenza/patterns/rhythm.py"
      provides: "euclidean_rhythm, binary_rhythm, apply_rhythm"
      exports: ["euclidean_rhythm", "binary_rhythm", "apply_rhythm"]
    - path: "src/cadenza/patterns/melodic.py"
      provides: "isorhythm, ostinato, accent_pattern"
      exports: ["isorhythm", "ostinato", "accent_pattern"]
    - path: "tests/patterns/test_rhythm.py"
      provides: "Tests for rhythm pattern functions"
    - path: "tests/patterns/test_melodic.py"
      provides: "Tests for melodic pattern functions"
  key_links:
    - from: "src/cadenza/patterns/rhythm.py"
      to: "cadenza.transforms.pitch.from_midi"
      via: "import for MIDI-to-Pitch conversion in apply_rhythm"
      pattern: "from cadenza\\.transforms\\.pitch import from_midi"
    - from: "src/cadenza/patterns/melodic.py"
      to: "cadenza.transforms.pitch.from_midi"
      via: "import for MIDI-to-Pitch conversion in isorhythm"
      pattern: "from cadenza\\.transforms\\.pitch import from_midi"
    - from: "src/cadenza/patterns/__init__.py"
      to: "cadenza.patterns.rhythm"
      via: "re-export"
      pattern: "from cadenza\\.patterns\\.rhythm import"
---

<objective>
Implement Euclidean rhythms, binary rhythms, apply_rhythm, isorhythm, ostinato, and accent patterns in a new `src/cadenza/patterns/` subpackage.

Purpose: Deliver six of eight pattern generation functions (the single-voice / single-phrase ones) with full test coverage.
Output: `src/cadenza/patterns/rhythm.py`, `src/cadenza/patterns/melodic.py`, package init, and tests.
</objective>

<execution_context>
@/Users/jor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-pattern-generation/10-CONTEXT.md
@.planning/phases/10-pattern-generation/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/cadenza/core/note.py:
```python
@dataclass(frozen=True)
class Note:
    pitch: Pitch
    duration: Duration
    dynamic: str | None = None
    articulations: tuple[str, ...] = ()

@dataclass(frozen=True)
class Rest:
    duration: Duration

Event: TypeAlias = Note | Rest
```

From src/cadenza/core/phrase.py:
```python
Phrase: TypeAlias = tuple[Event, ...]
```

From src/cadenza/core/duration.py:
```python
@dataclass(frozen=True)
class Duration:
    fraction: Fraction
    base: str = "q"
    dots: int = 0
    tuplet: int | None = None

    @staticmethod
    def from_cn(base: str, dots: int = 0, tuplet: int | None = None) -> Duration:
```

From src/cadenza/core/score.py:
```python
@dataclass(frozen=True)
class Score:
    _voices: tuple[tuple[str, Phrase], ...] = ()
```

From src/cadenza/transforms/pitch.py:
```python
def from_midi(midi_number: int, prefer_sharps: bool = True) -> Pitch:
```

From src/cadenza/counterpoint/__init__.py (subpackage pattern to follow):
```python
from cadenza.counterpoint.generation import (...)
from cadenza.counterpoint.validation import (...)
__all__ = [...]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Euclidean rhythm, binary rhythm, and apply_rhythm with tests</name>
  <files>
    src/cadenza/patterns/__init__.py,
    src/cadenza/patterns/rhythm.py,
    tests/patterns/__init__.py,
    tests/patterns/conftest.py,
    tests/patterns/test_rhythm.py
  </files>
  <read_first>
    src/cadenza/core/note.py,
    src/cadenza/core/phrase.py,
    src/cadenza/core/duration.py,
    src/cadenza/transforms/pitch.py,
    src/cadenza/counterpoint/__init__.py
  </read_first>
  <behavior>
    - euclidean_rhythm(3, 8) == (True, False, False, True, False, False, True, False) (tresillo)
    - euclidean_rhythm(5, 8) == (True, False, True, True, False, True, True, False) (cinquillo)
    - euclidean_rhythm(0, 8) == (False,)*8 (all rests)
    - euclidean_rhythm(8, 8) == (True,)*8 (all hits)
    - euclidean_rhythm(7, 16) has exactly 7 True values and 16 total slots
    - euclidean_rhythm(-1, 8) raises ValueError
    - euclidean_rhythm(9, 8) raises ValueError
    - binary_rhythm(0b10110) == (True, False, True, True, False)
    - binary_rhythm(0b1111) == (True, True, True, True)
    - binary_rhythm(0) == (False,) (single false slot for zero)
    - binary_rhythm(-1) raises ValueError
    - apply_rhythm pitches=(60,64,67) rhythm=(True,False,True,False,True) produces 3 Notes and 2 Rests
    - apply_rhythm with 3 pitches and 5 True slots cycles pitches: midi 60,64,67,60,64
    - apply_rhythm with all-False rhythm produces all Rests
    - apply_rhythm with slot_duration=Duration.from_cn("e") uses eighth note duration
    - apply_rhythm with default slot_duration uses quarter note duration (Fraction(1,4))
  </behavior>
  <action>
    1. Create `tests/patterns/__init__.py` (empty file).

    2. Create `tests/patterns/conftest.py` with shared fixtures:
       - `sample_phrase`: a 4-note Phrase using Pitches C4, D4, E4, F4 all with quarter duration
       - `quarter`: `Duration.from_cn("q")` fixture
       - Import helpers: `from cadenza.core.note import Note, Rest`, `from cadenza.core.pitch import Pitch`, `from cadenza.core.duration import Duration`, `from cadenza.transforms.pitch import from_midi`

    3. Create `tests/patterns/test_rhythm.py` with RED tests (import from `cadenza.patterns.rhythm`):
       - `test_euclidean_tresillo`: E(3,8) == (True, False, False, True, False, False, True, False)
       - `test_euclidean_cinquillo`: E(5,8) == (True, False, True, True, False, True, True, False)
       - `test_euclidean_all_rests`: E(0,8) == (False,)*8
       - `test_euclidean_all_hits`: E(8,8) == (True,)*8
       - `test_euclidean_7_16`: sum(E(7,16)) == 7 and len(E(7,16)) == 16
       - `test_euclidean_invalid_n_gt_m`: raises ValueError for n > m
       - `test_euclidean_invalid_negative`: raises ValueError for n < 0
       - `test_binary_rhythm_10110`: binary_rhythm(0b10110) == (True, False, True, True, False)
       - `test_binary_rhythm_all_ones`: binary_rhythm(0b1111) == (True,True,True,True)
       - `test_binary_rhythm_zero`: binary_rhythm(0) == (False,) -- single false slot
       - `test_binary_rhythm_negative`: raises ValueError
       - `test_apply_rhythm_basic`: pitches=(60,64,67), rhythm=(T,F,T,F,T) -> 3 Notes (C4,E4,G4) + 2 Rests at correct positions
       - `test_apply_rhythm_cycles_pitches`: pitches=(60,64,67), rhythm=(T,T,T,T,T) -> 5 Notes with MIDI 60,64,67,60,64
       - `test_apply_rhythm_all_rests`: rhythm all False -> all Rest objects
       - `test_apply_rhythm_custom_duration`: slot_duration=Duration.from_cn("e") -> all events have fraction == Fraction(1,8)
       - `test_apply_rhythm_default_quarter`: no slot_duration -> all events have fraction == Fraction(1,4)

    4. Create `src/cadenza/patterns/__init__.py` with re-exports (initially just rhythm):
       ```python
       from cadenza.patterns.rhythm import apply_rhythm, binary_rhythm, euclidean_rhythm
       __all__ = ["euclidean_rhythm", "binary_rhythm", "apply_rhythm"]
       ```

    5. Create `src/cadenza/patterns/rhythm.py` implementing:
       - `euclidean_rhythm(n: int, m: int) -> tuple[bool, ...]`: Bjorklund's algorithm as described in RESEARCH.md code example. Validate n >= 0, m > 0, n <= m.
       - `binary_rhythm(n: int) -> tuple[bool, ...]`: Convert integer `n` to binary, map each bit to bool. `n=0` returns `(False,)`. Validate n >= 0.
       - `apply_rhythm(pitches: tuple[int, ...], rhythm: tuple[bool, ...], slot_duration: Duration | None = None) -> Phrase`: For each True in rhythm, create `Note(pitch=from_midi(pitch_value), duration=dur)` consuming next pitch from `itertools.cycle(pitches)`. For each False, create `Rest(duration=dur)`. Default `dur = Duration.from_cn("q")` when slot_duration is None. Return `tuple(events)`.
       - Imports: `from __future__ import annotations`, `import itertools`, `from cadenza.core.duration import Duration`, `from cadenza.core.note import Note, Rest`, `from cadenza.core.phrase import Phrase`, `from cadenza.transforms.pitch import from_midi`

    6. Run tests, verify GREEN. Ensure `sum(euclidean_rhythm(3,8))` == 3 and `len(euclidean_rhythm(3,8))` == 8.
  </action>
  <verify>
    <automated>python3 -m pytest tests/patterns/test_rhythm.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/cadenza/patterns/rhythm.py` contains `def euclidean_rhythm(n: int, m: int) -> tuple[bool, ...]:`
    - `src/cadenza/patterns/rhythm.py` contains `def binary_rhythm(n: int) -> tuple[bool, ...]:`
    - `src/cadenza/patterns/rhythm.py` contains `def apply_rhythm(`
    - `src/cadenza/patterns/rhythm.py` contains `from cadenza.transforms.pitch import from_midi`
    - `src/cadenza/patterns/rhythm.py` contains `itertools.cycle`
    - `src/cadenza/patterns/__init__.py` contains `from cadenza.patterns.rhythm import`
    - `tests/patterns/test_rhythm.py` contains `test_euclidean_tresillo`
    - `tests/patterns/test_rhythm.py` contains `test_apply_rhythm_cycles_pitches`
    - `python3 -m pytest tests/patterns/test_rhythm.py -x -q` exits 0 with 0 failures
  </acceptance_criteria>
  <done>All three rhythm functions implemented and passing tests: euclidean_rhythm produces correct known patterns, binary_rhythm converts ints to bitmaps, apply_rhythm maps bitmaps+pitches to Phrases with cyclic extension.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isorhythm, ostinato, and accent pattern with tests</name>
  <files>
    src/cadenza/patterns/melodic.py,
    src/cadenza/patterns/__init__.py,
    tests/patterns/test_melodic.py
  </files>
  <read_first>
    src/cadenza/core/note.py,
    src/cadenza/core/duration.py,
    src/cadenza/core/phrase.py,
    src/cadenza/transforms/pitch.py,
    src/cadenza/patterns/rhythm.py,
    src/cadenza/patterns/__init__.py
  </read_first>
  <behavior>
    - isorhythm with talea length 3, color length 4, n=1 produces LCM(3,4)=12 notes
    - isorhythm cycles talea independently: note i gets talea[i % 3] and color[i % 4]
    - isorhythm with n=2 produces 24 notes (2 * LCM(3,4))
    - isorhythm with empty talea or color raises ValueError
    - isorhythm with n < 1 raises ValueError
    - ostinato(phrase, repeats=3) produces len(phrase)*3 events
    - ostinato(phrase, repeats=3, variation=fn) calls fn(phrase, i) for each repetition
    - ostinato with repeats < 1 raises ValueError
    - accent_pattern(phrase, n=2) adds 'accent' to articulations of every 2nd Note
    - accent_pattern skips Rests when counting (only counts Notes)
    - accent_pattern with n < 1 raises ValueError
  </behavior>
  <action>
    1. Create `tests/patterns/test_melodic.py` with RED tests (import from `cadenza.patterns.melodic`):
       - `test_isorhythm_basic_cycle`: talea=(q, e, e), color=(60, 64, 67, 72), n=1 -> 12 notes. Verify note[0] has duration q and pitch from_midi(60), note[3] has duration q (3%3=0) and pitch from_midi(72) (3%4=3), note[4] has duration e (4%3=1) and pitch from_midi(60) (4%4=0).
       - `test_isorhythm_two_cycles`: talea length 3, color length 4, n=2 -> 24 notes total.
       - `test_isorhythm_same_length`: talea and color both length 3, n=1 -> 3 notes (LCM(3,3)=3).
       - `test_isorhythm_empty_talea`: raises ValueError.
       - `test_isorhythm_empty_color`: raises ValueError.
       - `test_isorhythm_n_zero`: n=0 raises ValueError.
       - `test_ostinato_exact_repeat`: ostinato(phrase, repeats=3) -> 3*len(phrase) events, all events match original cyclically.
       - `test_ostinato_with_variation`: variation=lambda p, i: p if i == 0 else modified_phrase -> first repetition unchanged, subsequent modified.
       - `test_ostinato_repeats_zero`: raises ValueError.
       - `test_accent_pattern_every_2nd`: 4-note phrase, n=2 -> notes at index 1,3 have 'accent' in articulations.
       - `test_accent_pattern_skips_rests`: phrase with Rest at index 1, n=2 -> Rest unchanged, accent applied to 2nd Note not 2nd event.
       - `test_accent_pattern_n_zero`: raises ValueError.
       - `test_accent_pattern_preserves_existing_articulations`: Note with existing ('staccato',) gets ('staccato', 'accent') on accent.

    2. Create `src/cadenza/patterns/melodic.py` implementing:
       - `isorhythm(talea: tuple[Duration, ...], color: tuple[int, ...], n: int = 1) -> Phrase`:
         Validate talea and color non-empty, n >= 1. Compute `cycle_len = math.lcm(len(talea), len(color))`, `total = n * cycle_len`. For each i in range(total), create `Note(pitch=from_midi(color[i % len(color)]), duration=talea[i % len(talea)])`. Return `tuple(events)`.
         Imports: `import math`, `from cadenza.core.duration import Duration`, `from cadenza.core.note import Note`, `from cadenza.core.phrase import Phrase`, `from cadenza.transforms.pitch import from_midi`

       - `ostinato(phrase: Phrase, repeats: int, variation: Callable[[Phrase, int], Phrase] | None = None) -> Phrase`:
         Validate repeats >= 1, phrase non-empty. For i in range(repeats): if variation is None, append phrase; else append variation(phrase, i). Concatenate all into single tuple. Return tuple of all events.
         Import: `from collections.abc import Callable`

       - `accent_pattern(phrase: Phrase, n: int) -> Phrase`:
         Validate n >= 1. Iterate over events, maintain a note_count (only incremented for Note instances). When note_count % n == 0 (1-indexed: count starts at 1, accent when count % n == 0), use `dataclasses.replace(note, articulations=note.articulations + ('accent',))`. Pass through Rests unchanged. Return `tuple(events)`.
         Import: `from dataclasses import replace`, `from cadenza.core.note import Note, Rest, Event`

    3. Update `src/cadenza/patterns/__init__.py` to add melodic re-exports:
       ```python
       from cadenza.patterns.melodic import accent_pattern, isorhythm, ostinato
       from cadenza.patterns.rhythm import apply_rhythm, binary_rhythm, euclidean_rhythm

       __all__ = [
           "accent_pattern",
           "apply_rhythm",
           "binary_rhythm",
           "euclidean_rhythm",
           "isorhythm",
           "ostinato",
       ]
       ```

    4. Run tests, verify GREEN.
  </action>
  <verify>
    <automated>python3 -m pytest tests/patterns/test_melodic.py tests/patterns/test_rhythm.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `src/cadenza/patterns/melodic.py` contains `def isorhythm(talea: tuple[Duration, ...], color: tuple[int, ...], n: int = 1) -> Phrase:`
    - `src/cadenza/patterns/melodic.py` contains `def ostinato(`
    - `src/cadenza/patterns/melodic.py` contains `def accent_pattern(`
    - `src/cadenza/patterns/melodic.py` contains `from cadenza.transforms.pitch import from_midi`
    - `src/cadenza/patterns/melodic.py` contains `math.lcm`
    - `src/cadenza/patterns/melodic.py` contains `dataclasses` or `replace`
    - `src/cadenza/patterns/__init__.py` contains `from cadenza.patterns.melodic import`
    - `tests/patterns/test_melodic.py` contains `test_isorhythm_basic_cycle`
    - `tests/patterns/test_melodic.py` contains `test_ostinato_with_variation`
    - `tests/patterns/test_melodic.py` contains `test_accent_pattern_skips_rests`
    - `python3 -m pytest tests/patterns/test_melodic.py -x -q` exits 0 with 0 failures
    - `python3 -m pytest tests/patterns/ -x -q` exits 0 with 0 failures
  </acceptance_criteria>
  <done>Isorhythm cycles talea/color independently via LCM, ostinato repeats with optional variation callback, accent_pattern marks every Nth Note (skipping Rests). All tests green.</done>
</task>

</tasks>

<verification>
Run full pattern test suite:
```
python3 -m pytest tests/patterns/ -v --tb=short
```
Then run full project suite to check no regressions:
```
python3 -m pytest tests/ -v --tb=short
```
</verification>

<success_criteria>
- Six functions implemented: euclidean_rhythm, binary_rhythm, apply_rhythm, isorhythm, ostinato, accent_pattern
- All functions importable from `cadenza.patterns`
- All tests green in `tests/patterns/test_rhythm.py` and `tests/patterns/test_melodic.py`
- Full project test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/10-pattern-generation/10-01-SUMMARY.md`
</output>
