---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/cadenza/__init__.py
  - src/cadenza/core/__init__.py
  - src/cadenza/core/pitch.py
  - src/cadenza/core/duration.py
  - src/cadenza/core/interval.py
  - src/cadenza/core/note.py
  - src/cadenza/core/phrase.py
  - src/cadenza/core/score.py
  - src/cadenza/core/json_codec.py
  - src/cadenza/omn/__init__.py
  - src/cadenza/omn/errors.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/strategies.py
  - tests/core/__init__.py
  - tests/core/test_pitch.py
  - tests/core/test_duration.py
  - tests/core/test_interval.py
  - tests/core/test_note.py
  - tests/core/test_phrase.py
  - tests/core/test_score.py
  - tests/core/test_json_codec.py
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-04
  - CORE-05
  - CORE-06
  - CORE-07
  - CORE-08
  - CORE-09
  - CORE-10

must_haves:
  truths:
    - "Pitch('c', 'n', 4) creates an immutable C4 pitch object that is hashable and usable in sets/dicts"
    - "Pitch('e', 'b', 3) != Pitch('d', 's', 3) but Pitch('e', 'b', 3).enharmonic_equal(Pitch('d', 's', 3)) returns True"
    - "Duration.from_omn('q', dots=1).fraction == Fraction(3, 8) and never uses float"
    - "Note(pitch, duration, dynamic, articulations) is frozen/immutable with __hash__ and __eq__"
    - "Rest(duration) is a distinct type from Note"
    - "Phrase is a tuple of Note|Rest events, immutable and hashable"
    - "Score stores voices as tuple[tuple[str, Phrase], ...] for true immutability and hashability"
    - "All core types round-trip through JSON (to_json -> from_json) without information loss"
    - "Interval.between(Pitch('c','n',4), Pitch('e','n',4)) returns a major third"
  artifacts:
    - path: "src/cadenza/core/pitch.py"
      provides: "Pitch frozen dataclass with step/accidental/octave, midi_number, pitch_class, enharmonic_equal, ordering"
      exports: ["Pitch", "STEP_SEMITONES", "ACCIDENTAL_SEMITONES", "STEP_INDEX"]
    - path: "src/cadenza/core/duration.py"
      provides: "Duration frozen dataclass with Fraction arithmetic, from_omn factory"
      exports: ["Duration", "BASE_DURATIONS"]
    - path: "src/cadenza/core/interval.py"
      provides: "Interval frozen dataclass with quality/number/direction, between() factory"
      exports: ["Interval"]
    - path: "src/cadenza/core/note.py"
      provides: "Note and Rest frozen dataclasses, Event type alias"
      exports: ["Note", "Rest", "Event"]
    - path: "src/cadenza/core/phrase.py"
      provides: "Phrase type alias as tuple[Event, ...]"
      exports: ["Phrase"]
    - path: "src/cadenza/core/score.py"
      provides: "Score frozen dataclass with tuple-based voice storage"
      exports: ["Score"]
    - path: "src/cadenza/core/json_codec.py"
      provides: "JSON serialization/deserialization for all core types"
      exports: ["to_json", "from_json", "CadenzaEncoder", "cadenza_decoder"]
    - path: "pyproject.toml"
      provides: "Project config with zero runtime deps, dev deps for pytest/hypothesis/ruff/mypy"
      contains: "requires-python = \">=3.11\""
  key_links:
    - from: "src/cadenza/core/note.py"
      to: "src/cadenza/core/pitch.py"
      via: "Note.pitch field is Pitch type"
      pattern: "from cadenza\\.core\\.pitch import Pitch"
    - from: "src/cadenza/core/note.py"
      to: "src/cadenza/core/duration.py"
      via: "Note.duration and Rest.duration field is Duration type"
      pattern: "from cadenza\\.core\\.duration import Duration"
    - from: "src/cadenza/core/score.py"
      to: "src/cadenza/core/phrase.py"
      via: "Score._voices contains Phrase tuples"
      pattern: "from cadenza\\.core\\.phrase import Phrase"
    - from: "src/cadenza/core/json_codec.py"
      to: "src/cadenza/core/pitch.py"
      via: "Encoder/decoder handles Pitch serialization"
      pattern: "from cadenza\\.core\\.pitch import Pitch"
---

<objective>
Project scaffolding and complete core data model: Pitch, Duration, Interval, Note, Rest, Phrase, Score, plus JSON serialization codec.

Purpose: Establishes the immutable type system that every subsequent phase depends on. Correctness here prevents cascading failures in parser, transforms, analysis, and API layers.
Output: Installable Python package with all core types, full test suite, and JSON round-trip codec.
</objective>

<execution_context>
@/Users/jor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffolding + Pitch, Duration, Interval dataclasses with tests</name>
  <files>
    pyproject.toml
    src/cadenza/__init__.py
    src/cadenza/core/__init__.py
    src/cadenza/core/pitch.py
    src/cadenza/core/duration.py
    src/cadenza/core/interval.py
    src/cadenza/omn/__init__.py
    src/cadenza/omn/errors.py
    tests/__init__.py
    tests/conftest.py
    tests/strategies.py
    tests/core/__init__.py
    tests/core/test_pitch.py
    tests/core/test_duration.py
    tests/core/test_interval.py
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <behavior>
    Pitch tests (test_pitch.py):
    - Pitch('c', 'n', 4).midi_number == 60 (middle C = C4 = MIDI 60)
    - Pitch('a', 'n', 4).midi_number == 69
    - Pitch('e', 'b', 3) != Pitch('d', 's', 3) (spelling-sensitive equality)
    - Pitch('e', 'b', 3).enharmonic_equal(Pitch('d', 's', 3)) == True
    - Pitch('c', 'ss', 4).midi_number == Pitch('d', 'n', 4).midi_number == 62
    - Pitch('c', 'n', 4).pitch_class == 0
    - Pitch('f', 's', 5).pitch_class == 6
    - Pitch with invalid step raises ValueError
    - Pitch with invalid accidental raises ValueError
    - Pitch with octave out of range (-1..10) raises ValueError
    - Pitch is frozen (assignment raises FrozenInstanceError)
    - Pitch is hashable (usable in set and dict)
    - Two identical Pitch objects hash equally
    - Pitch ordering: lower MIDI number < higher MIDI number
    - hypothesis: arbitrary valid pitches have midi_number in 0..127 range

    Duration tests (test_duration.py):
    - Duration.from_omn('q').fraction == Fraction(1, 4)
    - Duration.from_omn('h').fraction == Fraction(1, 2)
    - Duration.from_omn('w').fraction == Fraction(1, 1)
    - Duration.from_omn('e').fraction == Fraction(1, 8)
    - Duration.from_omn('s').fraction == Fraction(1, 16)
    - Duration.from_omn('t').fraction == Fraction(1, 32)
    - Duration.from_omn('x').fraction == Fraction(1, 64)
    - Duration.from_omn('q', dots=1).fraction == Fraction(3, 8)
    - Duration.from_omn('h', dots=1).fraction == Fraction(3, 4)
    - Duration.from_omn('q', dots=2).fraction == Fraction(7, 16)
    - Duration.from_omn('q', tuplet=3).fraction == Fraction(1, 6) (triplet quarter)
    - Duration.from_omn('e', tuplet=3).fraction == Fraction(1, 12)
    - Duration.from_omn('q', tuplet=5).fraction == Fraction(1, 5) (quintuplet: 5 in time of 4)
    - Duration is frozen and hashable
    - Invalid base raises ValueError
    - Measure math: Fraction(3,8) + Fraction(1,8) + Fraction(1,2) == Fraction(1,1) (dotted-q + e + h = whole)
    - Duration ordering: shorter < longer by fraction value

    Interval tests (test_interval.py):
    - Interval.between(Pitch('c','n',4), Pitch('e','n',4)) -> quality='M', number=3, direction=1 (major third)
    - Interval.between(Pitch('c','n',4), Pitch('e','b',4)) -> quality='m', number=3 (minor third)
    - Interval.between(Pitch('c','n',4), Pitch('g','n',4)) -> quality='P', number=5 (perfect fifth)
    - Interval.between(Pitch('c','n',4), Pitch('f','n',4)) -> quality='P', number=4 (perfect fourth)
    - Interval.between(Pitch('c','n',4), Pitch('c','n',5)) -> quality='P', number=8 (octave)
    - Interval.between(Pitch('c','n',4), Pitch('c','n',4)) -> quality='P', number=1 (unison)
    - Interval.between(Pitch('c','n',4), Pitch('g','b',4)) -> quality='d', number=5 (diminished fifth)
    - Interval.between(Pitch('c','n',4), Pitch('f','s',4)) -> quality='A', number=4 (augmented fourth)
    - Interval is frozen and hashable
    - Compound intervals: Interval.between(C4, D5) -> number=9
  </behavior>
  <action>
    1. Create pyproject.toml at project root with:
       - name = "cadenza", version = "0.1.0", requires-python = ">=3.11"
       - ZERO runtime dependencies: dependencies = []
       - [project.optional-dependencies] dev = ["pytest>=8.0", "pytest-cov>=5.0", "hypothesis>=6.100", "ruff>=0.8", "mypy>=1.10"]
       - Build system: hatchling with packages = ["src/cadenza"]
       - [tool.pytest.ini_options] testpaths = ["tests"], addopts = "-v --tb=short"
       - [tool.ruff] target-version = "py311", line-length = 100
       - [tool.ruff.lint] select = ["E", "F", "I", "N", "UP", "B", "SIM", "TCH"]
       - [tool.mypy] python_version = "3.11", strict = true

    2. Create directory structure:
       - src/cadenza/__init__.py: version = "0.1.0", re-export core types
       - src/cadenza/core/__init__.py: re-export Pitch, Duration, Interval, Note, Rest, Event, Phrase, Score, to_json, from_json
       - src/cadenza/omn/__init__.py: empty for now (parser comes in plan 02)
       - src/cadenza/omn/errors.py: ParseError(Exception) frozen dataclass with message, position, line, column, source_snippet fields; ParseWarning dataclass with message, position, line, column fields

    3. Create src/cadenza/core/pitch.py:
       - Module-level constants:
         ACCIDENTAL_SEMITONES: dict[str, int] = {"bb": -2, "b": -1, "n": 0, "s": 1, "ss": 2}
         STEP_SEMITONES: dict[str, int] = {"c": 0, "d": 2, "e": 4, "f": 5, "g": 7, "a": 9, "b": 11}
         STEP_INDEX: dict[str, int] = {"c": 0, "d": 1, "e": 2, "f": 3, "g": 4, "a": 5, "b": 6}
       - @dataclass(frozen=True, order=False) class Pitch:
         Fields: step: str (lowercase a-g), accidental: str ("n","s","b","ss","bb"), octave: int (-1..10)
         __post_init__: validate step in STEP_SEMITONES, accidental in ACCIDENTAL_SEMITONES, -1 <= octave <= 10
         Property midi_number: (octave + 1) * 12 + STEP_SEMITONES[step] + ACCIDENTAL_SEMITONES[accidental]
         Property pitch_class: midi_number % 12
         Method enharmonic_equal(other: Pitch) -> bool: self.midi_number == other.midi_number
         Custom __lt__: compare by midi_number, then by STEP_INDEX for enharmonic ties
         Custom __le__, __gt__, __ge__ delegating to __lt__ and __eq__

    4. Create src/cadenza/core/duration.py:
       - BASE_DURATIONS: dict[str, Fraction] = {"w": Fraction(1,1), "h": Fraction(1,2), "q": Fraction(1,4), "e": Fraction(1,8), "s": Fraction(1,16), "t": Fraction(1,32), "x": Fraction(1,64)}
       - @dataclass(frozen=True, order=False) class Duration:
         Fields: fraction: Fraction, base: str = "q", dots: int = 0, tuplet: int | None = None
         __post_init__: validate base in BASE_DURATIONS, 0 <= dots <= 3, fraction > 0
         Static from_omn(base, dots=0, tuplet=None) -> Duration: compute fraction from base value, apply dot formula (each dot adds half previous increment), apply tuplet ratio (triplet: *Fraction(2,3), quintuplet: *Fraction(4,5), general: *Fraction(2^(ceil(log2(n))-1), n))
         Custom __lt__, __le__, __gt__, __ge__: compare by fraction value

    5. Create src/cadenza/core/interval.py:
       - PERFECT_INTERVALS: set[int] = {1, 4, 5, 8} (unison, fourth, fifth, octave -- mod 7 equivalents: 0, 3, 4, 0)
       - MAJOR_SCALE_SEMITONES: dict mapping generic interval (1-7) to semitone offset from root: {1: 0, 2: 2, 3: 4, 4: 5, 5: 7, 6: 9, 7: 11}
       - @dataclass(frozen=True) class Interval:
         Fields: quality: str ("P","M","m","A","d","AA","dd"), number: int (1+), direction: int (+1 or -1)
         Property semitones: compute from quality + number (for verification/utility)
         Static between(p1: Pitch, p2: Pitch) -> Interval:
           Algorithm:
           a. Compute direction from MIDI numbers (ascending if p2 >= p1, else descending; swap to always compute ascending)
           b. generic = (STEP_INDEX[upper.step] - STEP_INDEX[lower.step]) % 7 + 1
           c. octave_span = upper.octave - lower.octave; if upper step_index < lower step_index: octave_span -= 1
           d. number = generic + 7 * octave_span
           e. actual_semitones = abs(upper.midi_number - lower.midi_number)
           f. expected_semitones = MAJOR_SCALE_SEMITONES[generic_mod_7] + 12 * octave_span (where generic_mod_7 maps to 1-7 via ((number - 1) % 7) + 1)
           g. diff = actual_semitones - expected_semitones
           h. If generic is perfect type (1,4,5,8 mod 7): diff==0 -> "P", +1 -> "A", -1 -> "d", +2 -> "AA", -2 -> "dd"
           i. If generic is major type (2,3,6,7 mod 7): diff==0 -> "M", -1 -> "m", +1 -> "A", -2 -> "d", +2 -> "AA"

    6. Create tests/strategies.py with hypothesis strategies for Pitch, Duration, Note, Rest, Event, Phrase as specified in Research.

    7. Create tests/conftest.py with common fixtures: sample pitches (c4, eb3, fs5, etc.), sample durations (quarter, dotted_quarter, triplet_eighth), and pytest configuration.

    8. Write test files FIRST (RED), then implement (GREEN):
       - tests/core/test_pitch.py: all behaviors listed above
       - tests/core/test_duration.py: all behaviors listed above
       - tests/core/test_interval.py: all behaviors listed above

    9. Run `pip install -e ".[dev]"` to install package in dev mode, then run tests.
  </action>
  <verify>
    <automated>cd /Users/jor/dev/cadenza && pip install -e ".[dev]" && pytest tests/core/test_pitch.py tests/core/test_duration.py tests/core/test_interval.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File pyproject.toml exists with `name = "cadenza"` and `dependencies = []`
    - File src/cadenza/core/pitch.py contains `@dataclass(frozen=True` and `class Pitch`
    - `grep -c "enharmonic_equal" src/cadenza/core/pitch.py` returns at least 1
    - File src/cadenza/core/duration.py contains `from fractions import Fraction` and `class Duration`
    - `grep "BASE_DURATIONS" src/cadenza/core/duration.py` shows dict with "w", "h", "q", "e", "s", "t", "x"
    - File src/cadenza/core/interval.py contains `class Interval` with `between` static method
    - `pytest tests/core/test_pitch.py -x` exits 0
    - `pytest tests/core/test_duration.py -x` exits 0
    - `pytest tests/core/test_interval.py -x` exits 0
    - `python -c "from cadenza.core.pitch import Pitch; p = Pitch('c','n',4); assert p.midi_number == 60"` exits 0
    - `python -c "from cadenza.core.duration import Duration; from fractions import Fraction; d = Duration.from_omn('q', dots=1); assert d.fraction == Fraction(3,8)"` exits 0
  </acceptance_criteria>
  <done>Pitch, Duration, and Interval frozen dataclasses pass all unit tests. Pitch('e','b',3) != Pitch('d','s',3) but enharmonic_equal returns True. Duration uses Fraction exclusively. Interval.between computes correct quality from pitch spelling. All three types are frozen, hashable, and validated at construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Note, Rest, Phrase, Score dataclasses + JSON codec with tests</name>
  <files>
    src/cadenza/core/note.py
    src/cadenza/core/phrase.py
    src/cadenza/core/score.py
    src/cadenza/core/json_codec.py
    src/cadenza/core/__init__.py
    src/cadenza/__init__.py
    tests/core/test_note.py
    tests/core/test_phrase.py
    tests/core/test_score.py
    tests/core/test_json_codec.py
  </files>
  <read_first>
    src/cadenza/core/pitch.py
    src/cadenza/core/duration.py
    src/cadenza/core/interval.py
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <behavior>
    Note tests (test_note.py):
    - Note(Pitch('c','n',4), Duration.from_omn('q'), 'mf', ('stacc',)) creates valid immutable Note
    - Note is frozen (attribute assignment raises FrozenInstanceError)
    - Note is hashable and two identical Notes hash equally
    - Note with dynamic=None is valid (no dynamic)
    - Note with articulations=() is valid (no articulations)
    - Note.__eq__ compares all fields

    Rest tests (test_note.py):
    - Rest(Duration.from_omn('q')) creates valid immutable Rest
    - Rest is frozen and hashable
    - Rest is NOT a Note (isinstance check)
    - type(Rest(...)) != type(Note(...))

    Phrase tests (test_phrase.py):
    - A Phrase (tuple of Events) containing Notes and Rests is valid
    - Phrase is immutable (it is a tuple)
    - Phrase is hashable
    - Empty phrase tuple() is valid
    - Phrase preserves order

    Score tests (test_score.py):
    - Score.from_dict({"soprano": phrase1, "bass": phrase2}) creates Score with two voices
    - Score._voices is tuple[tuple[str, Phrase], ...]
    - Score.voices property returns dict
    - Score["soprano"] returns the soprano phrase
    - Score["nonexistent"] raises KeyError
    - Score.voice_names returns ("soprano", "bass") in insertion order
    - Score is frozen and hashable

    JSON codec tests (test_json_codec.py):
    - Pitch round-trips: from_json(to_json(pitch)) == pitch
    - Duration round-trips: from_json(to_json(duration)) == duration
    - Note round-trips: from_json(to_json(note)) == note
    - Rest round-trips: from_json(to_json(rest)) == rest
    - Phrase round-trips: from_json(to_json(phrase)) == phrase (list comparison, converted back to tuple)
    - Score round-trips through JSON
    - Fraction serialized as {"_type": "Fraction", "numerator": N, "denominator": D}
    - hypothesis property: arbitrary valid phrases round-trip through JSON
  </behavior>
  <action>
    1. Create src/cadenza/core/note.py:
       - from cadenza.core.pitch import Pitch
       - from cadenza.core.duration import Duration
       - @dataclass(frozen=True) class Note:
         Fields: pitch: Pitch, duration: Duration, dynamic: str | None = None, articulations: tuple[str, ...] = ()
       - @dataclass(frozen=True) class Rest:
         Fields: duration: Duration
       - Event = Note | Rest (type alias using Union for runtime, TypeAlias for typing)

    2. Create src/cadenza/core/phrase.py:
       - from cadenza.core.note import Event
       - Phrase = tuple[Event, ...] (TypeAlias)

    3. Create src/cadenza/core/score.py:
       - from cadenza.core.phrase import Phrase
       - @dataclass(frozen=True) class Score:
         Field: _voices: tuple[tuple[str, Phrase], ...] = ()
         Static from_dict(voices: dict[str, Phrase]) -> Score: return Score(_voices=tuple(voices.items()))
         Property voices -> dict[str, Phrase]: return dict(self._voices)
         Property voice_names -> tuple[str, ...]: return tuple(name for name, _ in self._voices)
         Method __getitem__(voice_name: str) -> Phrase: iterate _voices, raise KeyError if not found

    4. Create src/cadenza/core/json_codec.py:
       - CadenzaEncoder(json.JSONEncoder): handles Fraction (as _type/numerator/denominator), all dataclasses (as _type + asdict), and tuples (as {"_type": "tuple", "items": list})
       - cadenza_decoder(dct: dict): reconstruct from _type discriminator -- Fraction, Pitch, Duration, Note, Rest, Score, tuple
       - For Phrase (tuple of Events): serialize as JSON array; each element has _type "Note" or "Rest"
       - For Score: serialize _voices as list of [name, phrase] pairs
       - Public API: to_json(obj) -> str, from_json(s: str) -> Any
       - Handle nested reconstruction: Note contains Pitch and Duration; decode recursively

    5. Update src/cadenza/core/__init__.py to re-export: Pitch, Duration, Interval, Note, Rest, Event, Phrase, Score, to_json, from_json
    6. Update src/cadenza/__init__.py to expose version and re-export from core

    7. Write test files FIRST (RED), then implement (GREEN):
       - tests/core/test_note.py
       - tests/core/test_phrase.py
       - tests/core/test_score.py
       - tests/core/test_json_codec.py (include hypothesis round-trip property test)
  </action>
  <verify>
    <automated>cd /Users/jor/dev/cadenza && pytest tests/core/ -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File src/cadenza/core/note.py contains `class Note` and `class Rest` both with `@dataclass(frozen=True`
    - `grep "Event = " src/cadenza/core/note.py` shows `Note | Rest`
    - File src/cadenza/core/phrase.py contains `Phrase = tuple[Event, ...]`
    - File src/cadenza/core/score.py contains `class Score` with `_voices: tuple[tuple[str, Phrase], ...]`
    - `grep "from_dict" src/cadenza/core/score.py` returns at least 1 match
    - File src/cadenza/core/json_codec.py contains `to_json` and `from_json` and `CadenzaEncoder`
    - `pytest tests/core/ -x` exits 0 with all tests passing
    - `python -c "from cadenza.core import Pitch, Duration, Note, Rest, Phrase, Score, to_json, from_json"` exits 0
    - `python -c "from cadenza.core import Note, Pitch, Duration, to_json, from_json; n = Note(Pitch('c','n',4), Duration.from_omn('q'), 'mf', ('stacc',)); assert from_json(to_json(n)) == n"` exits 0
  </acceptance_criteria>
  <done>Note, Rest, Phrase, Score types are complete and frozen. JSON codec round-trips all core types without information loss. All core tests pass. The cadenza.core package exports everything needed for the OMN parser in plan 02.</done>
</task>

</tasks>

<verification>
- `pytest tests/core/ -v --cov=cadenza.core --cov-report=term-missing` shows all tests green with >90% coverage
- `python -c "from cadenza.core import Pitch, Duration, Interval, Note, Rest, Score; p = Pitch('e','b',3); q = Pitch('d','s',3); assert p != q; assert p.enharmonic_equal(q)"` exits 0
- `python -c "from fractions import Fraction; from cadenza.core import Duration; assert Duration.from_omn('q', dots=1).fraction + Duration.from_omn('e').fraction + Duration.from_omn('h').fraction == Fraction(1,1)"` exits 0
- `python -c "from cadenza.core import Note, Pitch, Duration, to_json, from_json; n = Note(Pitch('f','s',5), Duration.from_omn('e', dots=1), 'pp', ('trill',)); assert from_json(to_json(n)) == n"` exits 0
</verification>

<success_criteria>
All 10 CORE requirements (CORE-01 through CORE-10) are satisfied:
- CORE-01: Pitch is compound immutable (step, accidental, octave)
- CORE-02: Eb3 != D#3 by default; enharmonic_equal() compares MIDI numbers
- CORE-03: Duration uses Fraction internally, never float
- CORE-04: Interval has quality, number, direction; between() computes from spelling
- CORE-05: Note combines Pitch + Duration + dynamic + articulations as frozen dataclass
- CORE-06: Rest is distinct type from Note
- CORE-07: Phrase is immutable tuple of Events
- CORE-08: Score stores multiple named voices
- CORE-09: All types serialize to/from JSON losslessly
- CORE-10: All types support equality and hashing
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
