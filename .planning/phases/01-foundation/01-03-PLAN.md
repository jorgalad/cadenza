---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - tests/omn/test_round_trip.py
  - tests/test_integration.py
  - tests/strategies.py
autonomous: true
requirements:
  - CORE-01
  - CORE-02
  - CORE-03
  - CORE-05
  - CORE-06
  - CORE-07
  - CORE-09
  - CORE-10
  - NOTA-01
  - NOTA-02
  - NOTA-03
  - NOTA-04
  - NOTA-05
  - NOTA-06
  - NOTA-07
  - NOTA-12

must_haves:
  truths:
    - "Any valid Phrase round-trips through OMN: parse_omn(to_omn(phrase)) produces an equivalent Phrase"
    - "Any valid Phrase round-trips through JSON: from_json(to_json(phrase)) == phrase"
    - "OMN -> parse -> JSON -> from_json -> to_omn -> parse produces the same Phrase"
    - "Duration arithmetic in a full measure sums correctly with Fraction"
    - "Eb3 != D#3 throughout the entire pipeline (parse, serialize, JSON)"
    - "The full Phase 1 success criteria from ROADMAP.md are verified"
  artifacts:
    - path: "tests/omn/test_round_trip.py"
      provides: "Property-based round-trip tests for OMN parse/serialize"
      contains: "@given"
    - path: "tests/test_integration.py"
      provides: "End-to-end integration tests covering all Phase 1 success criteria"
      contains: "test_roadmap_criteria"
  key_links:
    - from: "tests/omn/test_round_trip.py"
      to: "src/cadenza/omn/parser.py"
      via: "Tests call parse_omn"
      pattern: "from cadenza\\.omn import parse_omn"
    - from: "tests/omn/test_round_trip.py"
      to: "src/cadenza/omn/serializer.py"
      via: "Tests call to_omn"
      pattern: "from cadenza\\.omn import to_omn"
    - from: "tests/test_integration.py"
      to: "src/cadenza/core/json_codec.py"
      via: "Tests verify JSON round-trip"
      pattern: "from cadenza\\.core import to_json, from_json"
---

<objective>
Integration tests, property-based round-trip tests, and ROADMAP success criteria verification.

Purpose: Validates that all Phase 1 components work together correctly. Property-based tests with Hypothesis catch edge cases that handwritten tests miss. Directly verifies the 5 success criteria from ROADMAP.md.
Output: Comprehensive test suite proving Phase 1 correctness end-to-end.
</objective>

<execution_context>
@/Users/jor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- All APIs used by integration tests -->

From src/cadenza/core:
```python
from cadenza.core import Pitch, Duration, Interval, Note, Rest, Event, Phrase, Score, to_json, from_json
```

From src/cadenza/omn:
```python
from cadenza.omn import parse_omn, to_omn, ParseError, ParseWarning
```

From tests/strategies.py:
```python
pitch_strategy()      # -> Pitch with valid step/acc/octave, MIDI 0-127
duration_strategy()   # -> Duration with valid base/dots/tuplet
note_strategy()       # -> Note with valid fields
rest_strategy()       # -> Rest with valid duration
event_strategy()      # -> Note | Rest
phrase_strategy()     # -> tuple[Event, ...] (1-20 events)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Property-based OMN round-trip tests + hypothesis strategy refinement</name>
  <files>
    tests/omn/test_round_trip.py
    tests/strategies.py
  </files>
  <read_first>
    tests/strategies.py
    src/cadenza/omn/parser.py
    src/cadenza/omn/serializer.py
    src/cadenza/omn/tokenizer.py
    src/cadenza/core/pitch.py
    src/cadenza/core/duration.py
    src/cadenza/core/note.py
  </read_first>
  <behavior>
    Round-trip property tests (test_round_trip.py):
    - @given(phrase_strategy()): parse_omn(to_omn(phrase)) produces a Phrase where each event has matching pitch, duration.fraction, dynamic, and articulations
      (Note: exact Duration equality may differ if base/dots representation changes, so compare fraction values)
    - @given(note_strategy()): a single Note serialized and re-parsed preserves all fields
    - @given(rest_strategy()): a single Rest serialized and re-parsed preserves duration
    - @given(phrase_strategy()): JSON round-trip: from_json(to_json(phrase)) == phrase (exact equality since JSON preserves all fields)
    - @given(phrase_strategy()): triple round-trip: parse_omn(to_omn(parse_omn(to_omn(phrase))[0])) == parse_omn(to_omn(phrase)) (idempotent after first round)
    - Specific edge cases as explicit tests:
      - Empty phrase round-trips
      - Single rest round-trips
      - Phrase with all different dynamics round-trips
      - Phrase with tuplet durations round-trips
      - Phrase with dotted durations round-trips
      - Phrase with double-sharp/double-flat pitches round-trips
      - Phrase mixing Notes and Rests round-trips
    - Cross-format: OMN -> parse -> JSON -> from_json -> to_omn -> parse gives same Phrase

    Strategy refinements (strategies.py updates):
    - Ensure generated pitches have accidental strings matching OMN vocabulary exactly
    - Ensure generated durations have valid base/dots/tuplet combinations that the serializer can emit
    - Add a phrase_with_rests_strategy that guarantees some Rests in the phrase
    - Ensure articulations drawn from KNOWN_ARTICULATIONS set (not arbitrary strings for round-trip tests)
  </behavior>
  <action>
    1. Read and update tests/strategies.py:
       - Verify pitch_strategy generates valid Pitch objects with MIDI 0-127
       - Verify duration_strategy generates Duration objects where from_omn round-trips cleanly
       - Verify note_strategy uses dynamics from the standard set (ppp,pp,p,mp,mf,f,ff,fff,None)
       - Verify note_strategy uses articulations from the known set: ("stacc","ten","acc","leg","marc","fermata","trill","pizz","arco")
       - Ensure articulations tuple generation draws 0-3 items without duplicates
       - Add phrase_with_rests_strategy that generates phrases containing at least one Rest

    2. Create tests/omn/test_round_trip.py:
       - Import from hypothesis: given, settings, HealthCheck
       - Import strategies from tests.strategies
       - Import parse_omn, to_omn from cadenza.omn
       - Import to_json, from_json from cadenza.core
       - Import Note, Rest, Pitch, Duration from cadenza.core

       Property tests:
       a. test_omn_round_trip_single_note:
          @given(note_strategy())
          def test(note):
            omn = to_omn((note,))
            parsed, warnings = parse_omn(omn)
            assert len(parsed) == 1
            result = parsed[0]
            assert isinstance(result, Note)
            assert result.pitch == note.pitch
            assert result.duration.fraction == note.duration.fraction
            assert result.dynamic == note.dynamic
            assert result.articulations == note.articulations

       b. test_omn_round_trip_single_rest:
          @given(rest_strategy())
          def test(rest):
            omn = to_omn((rest,))
            parsed, warnings = parse_omn(omn)
            assert len(parsed) == 1
            assert isinstance(parsed[0], Rest)
            assert parsed[0].duration.fraction == rest.duration.fraction

       c. test_omn_round_trip_phrase:
          @given(phrase_strategy())
          @settings(max_examples=200, suppress_health_check=[HealthCheck.too_slow])
          def test(phrase):
            if len(phrase) == 0:
              return  # skip empty
            omn_str = to_omn(phrase)
            parsed, warnings = parse_omn(omn_str)
            assert len(parsed) == len(phrase)
            for original, reparsed in zip(phrase, parsed):
              assert type(original) == type(reparsed)
              if isinstance(original, Note):
                assert reparsed.pitch == original.pitch
                assert reparsed.duration.fraction == original.duration.fraction
                assert reparsed.dynamic == original.dynamic
                assert reparsed.articulations == original.articulations
              else:  # Rest
                assert reparsed.duration.fraction == original.duration.fraction

       d. test_json_round_trip_phrase:
          @given(phrase_strategy())
          def test(phrase):
            json_str = to_json(phrase)
            restored = from_json(json_str)
            # JSON preserves exact representation
            assert tuple(restored) == phrase  (may need to convert list to tuple)

       e. test_omn_idempotent:
          @given(phrase_strategy())
          def test(phrase):
            if len(phrase) == 0:
              return
            omn1 = to_omn(phrase)
            p1, _ = parse_omn(omn1)
            omn2 = to_omn(p1)
            p2, _ = parse_omn(omn2)
            # After first round-trip, subsequent round-trips are stable
            assert omn2 == to_omn(p2)  # idempotent on serialized form

       f. test_cross_format_round_trip:
          @given(phrase_strategy())
          def test(phrase):
            if len(phrase) == 0:
              return
            # OMN -> JSON -> OMN path
            omn1 = to_omn(phrase)
            p1, _ = parse_omn(omn1)
            json_str = to_json(p1)
            p2 = tuple(from_json(json_str))  # convert list to tuple
            omn2 = to_omn(p2)
            p3, _ = parse_omn(omn2)
            # Compare structurally
            assert len(p1) == len(p3)
            for a, b in zip(p1, p3):
              assert type(a) == type(b)
              if isinstance(a, Note):
                assert a.pitch == b.pitch
                assert a.duration.fraction == b.duration.fraction
                assert a.dynamic == b.dynamic
                assert a.articulations == b.articulations

       Explicit edge case tests (not property-based):
       g. test_empty_phrase_round_trip: to_omn(()) -> "" -> parse_omn("") -> ()
       h. test_all_dynamics_round_trip: phrase with each of 8 dynamics
       i. test_tuplet_round_trip: "3q c4 d4 e4" round-trips
       j. test_dotted_round_trip: "q. c4 e. d4" round-trips
       k. test_double_accidentals_round_trip: "q css4 dbb3" round-trips
       l. test_mixed_notes_rests_round_trip: "q c4 -e q d4" round-trips
  </action>
  <verify>
    <automated>cd /Users/jor/dev/cadenza && pytest tests/omn/test_round_trip.py -x -v --timeout=60</automated>
  </verify>
  <acceptance_criteria>
    - File tests/omn/test_round_trip.py exists and contains `@given` decorators
    - `grep -c "@given" tests/omn/test_round_trip.py` returns at least 4 (property tests)
    - `pytest tests/omn/test_round_trip.py -x` exits 0
    - `pytest tests/omn/test_round_trip.py -v` shows at least 10 tests (6 property + 6 explicit)
    - No test takes longer than 60 seconds
  </acceptance_criteria>
  <done>Property-based round-trip tests pass with 200 examples per property. OMN parse/serialize, JSON encode/decode, and cross-format paths all preserve musical information. Hypothesis strategies generate valid Pitch, Duration, Note, Rest, and Phrase objects covering edge cases (double accidentals, tuplets, dots, rests).</done>
</task>

<task type="auto">
  <name>Task 2: Integration tests verifying all 5 ROADMAP success criteria</name>
  <files>
    tests/test_integration.py
  </files>
  <read_first>
    .planning/ROADMAP.md
    src/cadenza/core/pitch.py
    src/cadenza/core/duration.py
    src/cadenza/core/note.py
    src/cadenza/core/score.py
    src/cadenza/core/json_codec.py
    src/cadenza/omn/parser.py
    src/cadenza/omn/serializer.py
  </read_first>
  <action>
    Create tests/test_integration.py with explicit test functions mapping to each ROADMAP Phase 1 success criterion:

    1. test_roadmap_criterion_1_pitch_spelling_preservation():
       """A Pitch object preserves spelling (Eb3 and D#3 are distinct objects that compare unequal,
       but enharmonic_equal() returns True)."""
       eb3 = Pitch('e', 'b', 3)
       ds3 = Pitch('d', 's', 3)
       assert eb3 != ds3, "Eb3 and D#3 must be distinct (spelling-sensitive equality)"
       assert eb3.enharmonic_equal(ds3), "Eb3 and D#3 must be enharmonically equal"
       assert eb3.midi_number == ds3.midi_number, "Same MIDI number"
       # Verify through OMN pipeline
       phrase_eb, _ = parse_omn("q eb3")
       phrase_ds, _ = parse_omn("q ds3")
       assert phrase_eb[0].pitch != phrase_ds[0].pitch, "Spelling preserved through parser"
       assert phrase_eb[0].pitch.enharmonic_equal(phrase_ds[0].pitch)
       # Verify through JSON pipeline
       eb_json = to_json(eb3)
       ds_json = to_json(ds3)
       assert eb_json != ds_json, "JSON representations differ"
       assert from_json(eb_json) != from_json(ds_json), "JSON round-trip preserves distinction"
       assert from_json(eb_json).enharmonic_equal(from_json(ds_json))

    2. test_roadmap_criterion_2_duration_fraction_precision():
       """Duration arithmetic using Fraction never loses precision -- a measure of
       dotted-quarter + eighth + half sums to exactly Fraction(1, 1)."""
       dotted_q = Duration.from_omn('q', dots=1)  # 3/8
       eighth = Duration.from_omn('e')              # 1/8
       half = Duration.from_omn('h')                # 1/2
       total = dotted_q.fraction + eighth.fraction + half.fraction
       assert total == Fraction(1, 1), f"Expected exactly 1/1, got {total}"
       assert isinstance(total, Fraction), "Result must be Fraction, not float"
       # More complex: triplet arithmetic
       triplet_q = Duration.from_omn('q', tuplet=3)  # 1/6
       three_triplets = triplet_q.fraction * 3
       assert three_triplets == Fraction(1, 2), "Three triplet quarters = one half note"
       # Dotted half + quarter = whole
       dotted_h = Duration.from_omn('h', dots=1)  # 3/4
       quarter = Duration.from_omn('q')            # 1/4
       assert dotted_h.fraction + quarter.fraction == Fraction(1, 1)

    3. test_roadmap_criterion_3_omn_round_trip():
       """An OMN string like '(e f3 pp stacc)' round-trips through parse then serialize
       and produces an identical string."""
       # Note: the test input must use SPN (C4 = middle C), and the canonical
       # form uses the OMN conventions from CONTEXT.md
       test_cases = [
         "e c4 pp stacc",
         "e c4 pp stacc d4 e4",
         "q c4 mf e d4 pp stacc",
         "q. c4",
         "3q c4 d4 e4",
         "-q",
         "q c4 -e q d4",
       ]
       for omn_input in test_cases:
         phrase, warnings = parse_omn(omn_input)
         omn_output = to_omn(phrase)
         phrase2, warnings2 = parse_omn(omn_output)
         # Structural equality (not necessarily string equality for non-canonical inputs)
         assert len(phrase) == len(phrase2), f"Length mismatch for '{omn_input}'"
         for orig, reparsed in zip(phrase, phrase2):
           assert type(orig) == type(reparsed)
           if isinstance(orig, Note):
             assert orig.pitch == reparsed.pitch
             assert orig.duration.fraction == reparsed.duration.fraction
             assert orig.dynamic == reparsed.dynamic
             assert orig.articulations == reparsed.articulations
       # Canonical form exact string match
       canonical = "e c4 pp stacc d4 e4"
       phrase, _ = parse_omn(canonical)
       assert to_omn(phrase) == canonical, "Canonical OMN string must round-trip exactly"

    4. test_roadmap_criterion_4_parse_error_position():
       """The parser rejects malformed input with a clear error message including
       the position of the problem."""
       import pytest
       # Completely invalid token
       with pytest.raises(ParseError) as exc_info:
         parse_omn("q 123invalid")
       err = exc_info.value
       assert err.line >= 1, "Error must include line number"
       assert err.column >= 1, "Error must include column number"
       assert err.position >= 0, "Error must include character position"
       assert len(err.message) > 0, "Error must have descriptive message"
       # Another invalid input
       with pytest.raises(ParseError) as exc_info:
         parse_omn("q c4 pp stacc !!!")
       err = exc_info.value
       assert err.line >= 1
       assert err.column >= 1

    5. test_roadmap_criterion_5_hashable_comparable_json_serializable():
       """All core types (Pitch, Duration, Note, Rest, Phrase, Score) are hashable,
       comparable, and JSON-serializable without information loss."""
       # Build a complete musical example
       c4 = Pitch('c', 'n', 4)
       e4 = Pitch('e', 'n', 4)
       g4 = Pitch('g', 'n', 4)
       q = Duration.from_omn('q')
       e_dur = Duration.from_omn('e')

       note1 = Note(c4, q, 'mf', ('stacc',))
       note2 = Note(e4, e_dur, 'f', ())
       note3 = Note(g4, q, 'mf', ('ten',))
       rest = Rest(q)
       phrase: Phrase = (note1, note2, rest, note3)
       score = Score.from_dict({"melody": phrase})

       # Hashable: can be used in sets and dicts
       pitch_set = {c4, e4, g4, c4}  # c4 deduplicates
       assert len(pitch_set) == 3
       note_set = {note1, note2, note3}
       assert len(note_set) == 3
       # Phrase (tuple) is hashable
       phrase_set = {phrase, phrase}
       assert len(phrase_set) == 1
       # Score is hashable
       score_set = {score, score}
       assert len(score_set) == 1

       # Comparable: Pitch ordering
       assert c4 < e4 < g4

       # JSON serializable without info loss
       for obj, name in [(c4, "Pitch"), (q, "Duration"), (note1, "Note"), (rest, "Rest"), (score, "Score")]:
         json_str = to_json(obj)
         restored = from_json(json_str)
         assert restored == obj, f"{name} JSON round-trip failed"

       # Phrase JSON round-trip (tuple -> list -> tuple)
       phrase_json = to_json(phrase)
       restored_phrase = from_json(phrase_json)
       assert tuple(restored_phrase) == phrase, "Phrase JSON round-trip failed"

    6. test_integration_sticky_parameters():
       """Full integration test: sticky parameters carry through a complex example."""
       omn = "e c4 pp stacc d4 e4 q f4 mf ten g4 a4 ff"
       phrase, warnings = parse_omn(omn)
       assert len(phrase) == 6
       # c4: e, pp, stacc
       assert phrase[0].pitch == Pitch('c', 'n', 4)
       assert phrase[0].duration.base == 'e'
       assert phrase[0].dynamic == 'pp'
       assert phrase[0].articulations == ('stacc',)
       # d4: inherits e, pp, stacc
       assert phrase[1].duration.base == 'e'
       assert phrase[1].dynamic == 'pp'
       assert phrase[1].articulations == ('stacc',)
       # e4: inherits e, pp, stacc
       assert phrase[2].duration.base == 'e'
       assert phrase[2].dynamic == 'pp'
       assert phrase[2].articulations == ('stacc',)
       # f4: q (new), mf (new), ten (new)
       assert phrase[3].duration.base == 'q'
       assert phrase[3].dynamic == 'mf'
       assert phrase[3].articulations == ('ten',)
       # g4: inherits q, mf, ten
       assert phrase[4].duration.base == 'q'
       assert phrase[4].dynamic == 'mf'
       assert phrase[4].articulations == ('ten',)
       # a4: inherits q, ten; ff (new dynamic)
       assert phrase[5].dynamic == 'ff'
       assert phrase[5].articulations == ('ten',)

    7. test_integration_score_with_voices():
       """Score correctly stores and retrieves multiple voices."""
       soprano, _ = parse_omn("q c5 mf e5 g5 c6")
       bass, _ = parse_omn("h c3 mf g3")
       score = Score.from_dict({"soprano": soprano, "bass": bass})
       assert score.voice_names == ("soprano", "bass")
       assert score["soprano"] == soprano
       assert score["bass"] == bass
       # JSON round-trip
       score_json = to_json(score)
       restored = from_json(score_json)
       assert restored == score
  </action>
  <verify>
    <automated>cd /Users/jor/dev/cadenza && pytest tests/test_integration.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - File tests/test_integration.py exists with functions named test_roadmap_criterion_1 through test_roadmap_criterion_5
    - `grep -c "test_roadmap_criterion" tests/test_integration.py` returns at least 5
    - `pytest tests/test_integration.py -x` exits 0
    - `pytest tests/ -v --cov=cadenza --cov-report=term-missing` shows all tests green
    - Coverage of cadenza.core and cadenza.omn packages is above 85%
  </acceptance_criteria>
  <done>All 5 ROADMAP success criteria verified by explicit integration tests. Property-based round-trip tests pass with 200 examples. Full test suite (core + omn + integration + round-trip) runs green. Phase 1 foundation is complete and ready for Phase 2 transforms.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -v --cov=cadenza --cov-report=term-missing --timeout=60` all green with >85% coverage
- `pytest tests/test_integration.py -v` shows all 7 integration tests passing
- `pytest tests/omn/test_round_trip.py -v` shows all property-based tests passing
- `pytest tests/ -x --timeout=30` completes in under 30 seconds
</verification>

<success_criteria>
Phase 1 is verified end-to-end:
- Property-based tests confirm OMN round-trip for arbitrary valid phrases
- Property-based tests confirm JSON round-trip for arbitrary valid phrases
- Cross-format round-trip (OMN -> JSON -> OMN) preserves all information
- All 5 ROADMAP Phase 1 success criteria pass as explicit tests
- Sticky parameter behavior verified in complex multi-note examples
- Score multi-voice storage verified with JSON round-trip
- Full test suite runs in under 30 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
